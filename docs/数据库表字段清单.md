# 岗位闯关式实训平台 · 数据库字段清单（P0 · 单学院）

> 本文件由 `docs/database_schema_draft.sql` 解析生成，字段与 DDL 一一对应；
> 用途：逐字段评审。表结构说明与业务流程见 `docs/数据库表架构设计-雏形.md`。

- 主键：`id`，`bigint` 自增（`GENERATED BY DEFAULT AS IDENTITY`）
- 时间：`created_at` / `updated_at` 为 `timestamptz`，默认 `now()`
- 主数据表带 `deleted_at` 软删除；流水 / 快照表不删除
- 状态字段用 `varchar` 存 code，取值范围见附录 A

**表总数：38 张**（DDL 实际解析：38 张）

## 目录

| 域 | 表数 | 表 |
| --- | --- | --- |
| A 账户与权限 | 6 | `sys_user`、`sys_role`、`sys_permission`、`sys_user_role`、`sys_role_permission`、`system_config` |
| B 教学组织 | 4 | `class_info`、`class_group`、`class_student`、`class_student_group` |
| C 岗位与技能成长 | 9 | `job`、`student_job`、`skill_tree`、`skill_node`、`skill_node_dependency`、`student_skill`、`job_skill`、`project_skill`、`growth_rule` |
| D 实训项目 | 3 | `training_project`、`project_stage_template`、`project_module` |
| E 闯关过程与评审 | 8 | `file_asset`、`student_project`、`training_attempt`、`attempt_stage`、`attempt_stage_file`、`project_submission`、`review_record`、`review_ai_job` |
| F 证书台账 | 1 | `student_certificate` |
| H AI 问答知识库 | 5 | `knowledge_doc`、`knowledge_chunk`、`ai_qa_session`、`ai_qa_message`、`ai_qa_citation` |
| I 通知与审计 | 2 | `notification`、`operation_log` |

---

## 1. A 账户与权限

### 1.1 sys_user —— 平台用户

> 平台用户：单学院内学生/教师/管理员；密码只存哈希（PBKDF2-SHA256），新建账号默认密码 123456

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| user_no | varchar(50) | 否 | — | — | 学号/工号 |
| real_name | varchar(50) | 否 | — | — | 姓名 |
| user_type | varchar(20) | 否 | — | — | 用户类型（STUDENT 学生 / TEACHER 教师 / ADMIN 管理员） |
| ext_uid | varchar(64) | 是 | — | — | 学校 IDP 唯一标识 |
| major_name | varchar(100) | 是 | — | — | 专业（学生信息/名单导入） |
| avatar_url | varchar(255) | 是 | — | — | 头像地址 |
| phone | varchar(32) | 是 | — | — | 手机号 |
| email | varchar(128) | 是 | — | — | 邮箱 |
| status | varchar(20) | 否 | 'ACTIVE' | — | 账号状态（ACTIVE 正常 / DISABLED 停用） |
| last_login_at | timestamptz | 是 | — | — | 最近登录时间 |
| remark | varchar(255) | 是 | — | — | 备注 |
| password_hash | varchar(128) | 是 | — | — | 密码哈希（`pbkdf2_sha256$迭代次数$盐$摘要`）；为空表示从未设置密码 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**表级约束**：
- `CONSTRAINT uk_sys_user_no UNIQUE (user_no)`

**索引**：
- `UNIQUE uk_sys_user_ext (ext_uid) WHERE ext_uid IS NOT NULL`
- `idx_sys_user_type_status (user_type, status)`


### 1.2 sys_role —— 角色

> 角色定义（教师/学生/管理员）；密码统一在 sys_user 上，角色不存密码

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| role_code | varchar(50) | 否 | — | — | 角色编码（唯一） |
| role_name | varchar(50) | 否 | — | — | 角色名称 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_sys_role_code UNIQUE (role_code)`


### 1.3 sys_permission —— 权限点

> 权限点（扁平结构，无父子层级、无排序）

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| perm_code | varchar(100) | 否 | — | — | 权限编码（唯一，如 PROJECT_MANAGE / REVIEW_HANDLE） |
| perm_name | varchar(100) | 否 | — | — | 权限名称 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_sys_perm_code UNIQUE (perm_code)`


### 1.4 sys_user_role —— 用户-角色关系

> 用户与角色的多对多关系（含分配人）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| user_id | bigint | 否 | — | FK → sys_user.id | 用户 ID（sys_user.id） |
| role_id | bigint | 否 | — | FK → sys_role.id | 角色 ID（sys_role.id） |
| assigned_by | bigint | 是 | — | FK → sys_user.id | 分配人 ID（sys_user.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_sys_user_role UNIQUE (user_id, role_id)`


### 1.5 sys_role_permission —— 角色-权限关系

> 角色与权限点的多对多关系。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| role_id | bigint | 否 | — | FK → sys_role.id | 角色 ID（sys_role.id） |
| permission_id | bigint | 否 | — | FK → sys_permission.id | 权限 ID（sys_permission.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_sys_role_perm UNIQUE (role_id, permission_id)`


### 1.6 system_config —— 系统配置

> 平台级键值配置（等级阈值、AI 模型参数、证书编号规则等）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| config_key | varchar(100) | 否 | — | — | 配置键（唯一） |
| config_value | jsonb | 否 | '{}' | — | 配置值（JSON） |
| description | varchar(255) | 是 | — | — | 配置说明 |
| updated_by | bigint | 是 | — | FK → sys_user.id | 最后修改人 ID（sys_user.id） |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_system_config_key UNIQUE (config_key)`


## 2. B 教学组织

### 2.1 class_info —— 班级

> 班级主数据：班级名称、负责教师、分组数量。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| class_name | varchar(100) | 否 | — | — | 班级名称 |
| grade_year | smallint | 是 | — | — | 届/年级 |
| head_teacher_id | bigint | 是 | — | FK → sys_user.id | 负责教师 |
| group_count | smallint | 否 | 1 | — | 列表展示冗余 |
| remark | varchar(255) | 是 | — | — | 备注 |
| status | varchar(20) | 否 | 'ACTIVE' | — | 班级状态（ACTIVE 在读 / ARCHIVED 归档） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**索引**：
- `idx_class_info_teacher (head_teacher_id)`


### 2.2 class_group —— 班级分组

> 班级内部的分组（第 1 组、第 2 组…），教师可调整。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| class_id | bigint | 否 | — | FK → class_info.id | 班级 ID（class_info.id） |
| group_no | smallint | 否 | — | — | 分组序号 |
| group_name | varchar(50) | 否 | — | — | 分组名称 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_class_group_no UNIQUE (class_id, group_no)`

**索引**：
- `idx_class_group_class (class_id)`


### 2.3 class_student —— 在班学生

> 学生在班关系，含转出历史；任务发布按 ENROLLED 状态取人。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| class_id | bigint | 否 | — | FK → class_info.id | 班级 ID（class_info.id） |
| student_id | bigint | 否 | — | FK → sys_user.id | 学生 ID（sys_user.id） |
| status | varchar(20) | 否 | 'ENROLLED' | — | 在班状态（ENROLLED 在班 / LEFT 已离班） |
| enrolled_at | timestamptz | 否 | now() | — | 入班时间 |
| left_at | timestamptz | 是 | — | — | 离班 / 转出时间（为空表示仍在班） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**索引**：
- `UNIQUE uk_class_student_active (class_id, student_id) WHERE status = 'ENROLLED'`
- `idx_class_student_student (student_id, status)`


### 2.4 class_student_group —— 学生所在分组

> 学生当前所在分组（调整分组即更新本表）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| class_student_id | bigint | 否 | — | FK → class_student.id、UQ | 在班记录 ID（class_student.id） |
| group_id | bigint | 否 | — | FK → class_group.id | 分组 ID（class_group.id） |
| assigned_at | timestamptz | 否 | now() | — | 分配时间 |
| assigned_by | bigint | 是 | — | FK → sys_user.id | 分配人 ID（sys_user.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**索引**：
- `idx_class_student_group_group (group_id)`


## 3. C 岗位与技能成长

### 3.1 job —— 岗位

> 岗位主数据（方向、推荐等级、适配场景、热度、启用状态）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| job_name | varchar(100) | 否 | — | — | 岗位名称 |
| direction_tag | varchar(50) | 是 | — | — | 岗位方向标签（如“工业视觉”） |
| recommended_level | varchar(20) | 是 | — | — | 推荐学习等级（BASIC 基础 / ADVANCED 进阶 / EXPANDED 拓展） |
| scene | varchar(100) | 是 | — | — | 适配实训场景 |
| description | text | 是 | — | — | 说明 / 描述 |
| heat | bigint | 否 | 0 | — | 热度（事件更新） |
| status | varchar(20) | 否 | 'ENABLED' | — | 岗位状态（ENABLED 启用 / DISABLED 停用） |
| created_by | bigint | 是 | — | FK → sys_user.id | 创建人 ID（sys_user.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**表级约束**：
- `CONSTRAINT uk_job_name UNIQUE (job_name)`

**索引**：
- `idx_job_status (status, recommended_level)`


### 3.2 student_job —— 学生-岗位选择

> 学生选择的岗位，支持多岗位与主岗位标记，切换岗位保留技能进度。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| student_id | bigint | 否 | — | FK → sys_user.id | 学生 ID（sys_user.id） |
| job_id | bigint | 否 | — | FK → job.id | 岗位 ID（job.id） |
| is_primary | boolean | 否 | false | — | 是否当前主岗位（每名学生至多一个） |
| selected_at | timestamptz | 否 | now() | — | 选择时间 |
| switched_at | timestamptz | 是 | — | — | 切换时间 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_student_job UNIQUE (student_id, job_id)`

**索引**：
- `UNIQUE uk_student_job_primary (student_id) WHERE is_primary`
- `idx_student_job_student (student_id, is_primary)`


### 3.3 skill_tree —— 技能树体系

> 技能树的四大体系（光学成像系 / 传统算法系 / 深度学习系 / 系统部署系）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| tree_code | varchar(50) | 否 | — | — | 技能树编码（唯一） |
| tree_name | varchar(100) | 否 | — | — | 技能树名称 |
| description | varchar(255) | 是 | — | — | 说明 / 描述 |
| status | varchar(20) | 否 | 'ENABLED' | — | 技能树状态（ENABLED / DISABLED） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**表级约束**：
- `CONSTRAINT uk_skill_tree_code UNIQUE (tree_code)`


### 3.4 skill_node —— 技能节点（技能点）

> 技能节点（技能点），技能树上的最小能力单元。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| tree_id | bigint | 否 | — | FK → skill_tree.id | 技能树 ID（skill_tree.id） |
| node_code | varchar(50) | 否 | — | — | 技能节点编码（唯一） |
| node_name | varchar(100) | 否 | — | — | 技能节点名称 |
| description | text | 是 | — | — | 说明 / 描述 |
| unlock_note | varchar(255) | 是 | — | — | 给学生看的解锁说明文案 |
| unlock_rule_json | jsonb | 否 | '{}' | — | 未解锁提示用的规则（JSON），权威关系在 project_skill |
| status | varchar(20) | 否 | 'ENABLED' | — | 技能节点状态（ENABLED / DISABLED） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**表级约束**：
- `CONSTRAINT uk_skill_node_code UNIQUE (node_code)`

**索引**：
- `idx_skill_node_tree (tree_id)`


### 3.5 skill_node_dependency —— 技能前置依赖

> 技能点之间的前置依赖（DAG），决定“先学什么才能学什么”。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| node_id | bigint | 否 | — | FK → skill_node.id | 技能节点 ID（skill_node.id） |
| prerequisite_node_id | bigint | 否 | — | FK → skill_node.id | 前置技能节点 ID（skill_node.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_skill_node_dep UNIQUE (node_id, prerequisite_node_id)`
- `CONSTRAINT chk_skill_node_not_self CHECK (node_id <> prerequisite_node_id)`


### 3.6 student_skill —— 学生技能进度

> 学生每个技能点的进度（0~100）；不再分状态，前端按进度展示。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| student_id | bigint | 否 | — | FK → sys_user.id | 学生 ID（sys_user.id） |
| skill_node_id | bigint | 否 | — | FK → skill_node.id | 技能节点 ID（skill_node.id） |
| level | smallint | 否 | 0 | — | 熟练等级（预留：三态模式下可用 0/1/2） |
| progress | numeric(5,2) | 否 | 0 | — | 技能进度（0~100，= 完成数 ÷ 关联项目总数） |
| activated_at | timestamptz | 是 | — | — | 首次产生进度的时间（进度归零时清空） |
| mastered_at | timestamptz | 是 | — | — | 首次达到 100% 的时间（进度回落时清空） |
| source | varchar(30) | 是 | — | — | 进度来源（PROJECT / REVIEW / MANUAL） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_student_skill UNIQUE (student_id, skill_node_id)`

**索引**：
- `idx_student_skill_node (skill_node_id)`


### 3.7 job_skill —— 岗位-技能要求

> 岗位-技能关联；不设要求程度/权重，岗位推荐按学生技能进度计算

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| job_id | bigint | 否 | — | FK → job.id | 岗位 ID（job.id） |
| skill_node_id | bigint | 否 | — | FK → skill_node.id | 技能节点 ID（skill_node.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_job_skill UNIQUE (job_id, skill_node_id)`

**索引**：
- `idx_job_skill_skill (skill_node_id)`


### 3.8 project_skill —— 项目-技能关系

> 项目-技能多对多关系：完成关联项目即推进该技能进度（完成数/关联项目总数）

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| project_id | bigint | 否 | — | FK → training_project.id | 项目 ID（training_project.id） |
| skill_node_id | bigint | 否 | — | FK → skill_node.id | 技能节点 ID（skill_node.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_project_skill UNIQUE (project_id, skill_node_id)`

**索引**：
- `idx_project_skill_node (skill_node_id)`


### 3.9 growth_rule —— 成长规则

> 按实训层级统一配置解锁条件、技能书最大等级、项目及格线与等级说明

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| level_type | varchar(20) | 否 | — | — | 实训层级（BASIC 基础 / ADVANCED 进阶 / EXPANDED 拓展） |
| unlock_condition_json | jsonb | 否 | '{}' | — | 该层级的解锁条件 |
| skill_max_level | smallint | 否 | 1 | — | 技能书最大等级（等级上限） |
| pass_score | numeric(5,2) | 否 | 60 | — | 该层级项目的完成及格线（同层级所有项目共用） |
| level_description | text | 是 | — | — | 等级说明 |
| enabled | boolean | 否 | true | — | 该层级成长规则是否启用 |
| updated_by | bigint | 是 | — | FK → sys_user.id | 最后修改人 ID（sys_user.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_growth_rule_level UNIQUE (level_type)`


## 4. D 实训项目

### 4.1 training_project —— 实训项目

> 实训项目主数据；完成及格线不在此配置，统一读 growth_rule.pass_score

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| project_name | varchar(150) | 否 | — | — | 项目名称 |
| project_level | varchar(20) | 否 | — | — | 项目层级（BASIC 基础 / ADVANCED 进阶 / EXPANDED 拓展） |
| difficulty | smallint | 否 | 3 | — | 难度（1~5，1 最简单、5 最难） |
| job_id | bigint | 是 | — | FK → job.id | 绑定岗位（可空） |
| description | text | 是 | — | — | 说明 / 描述 |
| status | varchar(20) | 否 | 'DRAFT' | — | 项目状态（DRAFT 草稿 / PUBLISHED 已发布 / OFF_SHELF 下架） |
| creator_id | bigint | 是 | — | FK → sys_user.id | 创建人 ID（sys_user.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**表级约束**：
- `CONSTRAINT uk_training_project_name UNIQUE (project_name)`
- `CONSTRAINT chk_training_project_difficulty CHECK (difficulty BETWEEN 1 AND 5)`

**索引**：
- `idx_training_project_status (project_level, status)`


### 4.2 project_stage_template —— 标准化模块库

> 标准化模块库：关卡的唯一来源，教师可自定义增删（**软删**），项目只能从这里挑模板；
> 模板只定义通用信息（编码/名称/默认权重/默认要求），**不含填写子标题** —— 子标题由教师在各项目里手填

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| stage_key | varchar(50) | 否 | — | — | 模块类型编码（七大模块之一） |
| stage_name | varchar(100) | 否 | — | — | 关卡 / 模块名称 |
| description | text | 是 | — | — | 说明 / 描述 |
| default_required | boolean | 否 | true | — | 默认是否必填 |
| default_weight | numeric(5,2) | 否 | 0 | — | 默认分值占比 |
| default_requirement | text | 是 | — | — | 默认作答要求 |
| default_accept_standard | text | 是 | — | — | 默认验收标准 |
| sort_no | int | 否 | 0 | — | 排序号（越小越靠前） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删时间（为空表示未删除；删除后不再出现在模块库列表里） |

**表级约束**：
- `CONSTRAINT uk_stage_template_key UNIQUE (stage_key)`


### 4.3 project_module —— 项目模块组成

> 项目模块组成：记录项目**选中的模块库模板**，一条 = 一个关卡；没选中的模板不落库（所以没有 enabled 字段）。
> 关卡定义（编码、名称、默认要求与标准）统一读 project_stage_template，要新增关卡必须先加进模块库

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| project_id | bigint | 否 | — | FK → training_project.id | 项目 ID（training_project.id） |
| template_id | bigint | 否 | — | FK → project_stage_template.id | 选中的模块库模板 ID（关卡只能从模块库选，必填） |
| stage_no | smallint | 否 | — | — | 项目内填写顺序（从 1 开始） |
| items_json | jsonb | 否 | '[]' | — | 填写引导子标题，形如 `[{"title":"检测对象描述","prompt":"工件与精度"}]`；**由教师在项目里手填**（模板不预设），只做引导，不参与校验与评分 |
| requirement | text | 是 | — | — | 该模块的作答要求（可覆盖模板默认值） |
| accept_standard | text | 是 | — | — | 该模块的验收标准（可覆盖模板默认值） |
| required | boolean | 否 | true | — | 该模块是否必填（必填模块全部填写完成才允许整单提交） |
| weight | numeric(5,2) | 否 | 0 | — | 该模块分值占比（本项目选中的模块合计 100，供 AI 评审计分参考） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_project_module_template UNIQUE (project_id, template_id)`
- `CONSTRAINT uk_project_module_no UNIQUE (project_id, stage_no)`

**索引**：
- `idx_project_module_project (project_id, stage_no)`

### 4.4 project_file —— 项目附件

> 项目附件：报告模板、数据文件、说明文档等；文件本体在存储里（bucket + object_key），这里只存关联

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| project_id | bigint | 否 | — | FK → training_project.id | 项目 ID |
| file_asset_id | bigint | 否 | — | FK → file_asset.id | 文件台账 ID（上传后得到） |
| file_kind | varchar(30) | 否 | — | — | 用途（REPORT_TEMPLATE 报告模板 / DATASET 数据文件 / GUIDE 说明文档 / SCORING_CRITERIA 评分标准 / OTHER 其它） |
| title | varchar(150) | 是 | — | — | 展示名称，留空用原始文件名 |
| remark | varchar(255) | 是 | — | — | 备注 |
| sort_no | int | 否 | 0 | — | 展示排序，值越小越靠前 |
| uploaded_by | bigint | 是 | — | FK → sys_user.id | 上传人 ID |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_project_file UNIQUE (project_id, file_asset_id)`（同一文件在一个项目里只挂一次）

**索引**：
- `idx_project_file_project (project_id, file_kind)`


## 5. E 闯关过程与评审

### 5.1 file_asset —— 文件元数据

> 文件元数据（MinIO 对象）的登记表：提交附件、头像、封面、证书文件、导入名单等。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| uploader_id | bigint | 是 | — | FK → sys_user.id | 上传人 ID（sys_user.id） |
| bucket | varchar(100) | 否 | — | — | 对象存储桶（MinIO bucket） |
| object_key | varchar(255) | 否 | — | — | 对象存储 Key |
| original_name | varchar(255) | 否 | — | — | 原始文件名 |
| content_type | varchar(100) | 是 | — | — | 文件类型（MIME） |
| size_bytes | bigint | 否 | 0 | — | 文件大小（字节） |
| biz_type | varchar(30) | 否 | — | — | 业务类型（SUBMISSION 提交附件 / AVATAR 头像 / PROJECT_COVER 封面 / CERT_PDF 证书 / IMPORT 导入名单 / REPORT 报告） |
| status | varchar(20) | 否 | 'ACTIVE' | — | ACTIVE/INVALID |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_file_asset_object UNIQUE (bucket, object_key)`


### 5.2 student_project —— 学生实训记录

> 学生实训记录：学生点击“开始闯关”时生成一条，承载填写进度、成绩与完成状态

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| student_id | bigint | 否 | — | FK → sys_user.id | 学生 ID（sys_user.id） |
| project_id | bigint | 否 | — | FK → training_project.id | 项目 ID（training_project.id） |
| status | varchar(20) | 否 | 'NOT_STARTED' | — | 学生实训状态（NOT_STARTED 未开始 / IN_PROGRESS 填写中 / SUBMITTED 已提交待评审 / COMPLETED 已完成） |
| progress | numeric(5,2) | 否 | 0 | — | 填写进度（0~100，已填写模块 ÷ 启用模块） |
| total_score | numeric(5,2) | 是 | — | — | 当前总分（最新轮次成绩） |
| best_score | numeric(5,2) | 是 | — | — | 历史最高总分 |
| attempt_count | smallint | 否 | 0 | — | 闯关轮次数量 |
| started_at | timestamptz | 是 | — | — | 开始时间 |
| completed_at | timestamptz | 是 | — | — | 完成时间 |
| completed_score | numeric(5,2) | 是 | — | — | 完成判定通过时的总分快照 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_student_project UNIQUE (student_id, project_id)`

**索引**：
- `idx_student_project_student (student_id, status)`
- `idx_student_project_project (project_id)`


### 5.3 training_attempt —— 闯关轮次

> 闯关轮次：重新挑战生成新的 attempt_no，历史保留

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| student_project_id | bigint | 否 | — | FK → student_project.id | 学生实训记录 ID（student_project.id） |
| attempt_no | smallint | 否 | — | — | 轮次序号（从 1 递增） |
| status | varchar(20) | 否 | 'IN_PROGRESS' | — | 轮次状态（IN_PROGRESS 填写中 / SUBMITTED 已提交 / COMPLETED 已完成 / ABANDONED 已放弃） |
| filled_stage_count | smallint | 否 | 0 | — | 本轮已填写模块数 |
| submitted_at | timestamptz | 是 | — | — | 整单提交时间 |
| finished_at | timestamptz | 是 | — | — | 评审结束时间 |
| total_score | numeric(5,2) | 是 | — | — | 本轮总分 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_training_attempt UNIQUE (student_project_id, attempt_no)`

**索引**：
- `idx_training_attempt_project (student_project_id, status)`


### 5.4 attempt_stage —— 模块作答

> 轮次内每个模块的作答与填写状态；全部必填模块填写完成后才允许整单提交

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| attempt_id | bigint | 否 | — | FK → training_attempt.id | 闯关轮次 ID（training_attempt.id） |
| project_module_id | bigint | 否 | — | FK → project_module.id | 项目模块 ID（project_module.id） |
| is_filled | boolean | 否 | false | — | 该模块是否已填写完成 |
| answer_text | text | 是 | — | — | 该模块的作答文本内容 |
| filled_at | timestamptz | 是 | — | — | 该模块填写完成时间 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_attempt_stage UNIQUE (attempt_id, project_module_id)`

**索引**：
- `idx_attempt_stage_module (project_module_id)`


### 5.5 attempt_stage_file —— 模块作答附件

> 模块作答与文件的关联（一个模块可带多个附件）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| attempt_stage_id | bigint | 否 | — | FK → attempt_stage.id | 模块作答 ID（attempt_stage.id） |
| file_asset_id | bigint | 否 | — | FK → file_asset.id | 文件 ID（file_asset.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**表级约束**：
- `CONSTRAINT uk_attempt_stage_file UNIQUE (attempt_stage_id, file_asset_id)`


### 5.6 project_submission —— 整单提交

> 整单提交记录：一次提交包含该轮次全部模块的作答，可多次重提，学生可撤回

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| attempt_id | bigint | 否 | — | FK → training_attempt.id | 闯关轮次 ID（training_attempt.id） |
| submit_no | smallint | 否 | — | — | 整单提交序号（从 1 递增，重新提交 +1） |
| status | varchar(20) | 否 | 'PENDING_AI' | — | 评审状态（PENDING_AI 待AI评审 / AI_PASSED AI通过 / AI_FAILED AI未通过 / PENDING_REVIEW 待复审 / REVIEWING 复审中 / REVIEWED 已复审 / WITHDRAWN 已撤回） |
| final_conclusion | varchar(10) | 是 | — | — | 最终结论（PASS 通过 / FAIL 不通过） |
| total_score | numeric(5,2) | 是 | — | — | 本次提交总分（0~100） |
| objection_reason | varchar(500) | 是 | — | — | 学生对 AI 结果的异议说明（申请教师复审时填写） |
| is_starred | boolean | 否 | false | — | 教师标星（标星后进入审核列表关注区） |
| submitted_at | timestamptz | 否 | now() | — | 整单提交时间 |
| withdrawn_at | timestamptz | 是 | — | — | 学生撤回时间（撤回后实训记录回到填写中） |
| reviewed_at | timestamptz | 是 | — | — | 评审完成时间 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_project_submission UNIQUE (attempt_id, submit_no)`

**索引**：
- `idx_project_submission_status (status, submitted_at DESC)`
- `idx_project_submission_starred (is_starred, submitted_at DESC) WHERE is_starred`


### 5.7 review_record —— 评审记录（AI/教师）

> 整单评审记录（AI/教师）；维度明细以 JSON 存储，不再单独建维度字典表

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| submission_id | bigint | 否 | — | FK → project_submission.id | 整单提交 ID（project_submission.id） |
| review_kind | varchar(20) | 否 | — | — | 评审类型（AI 自动评审 / TEACHER 教师复审） |
| version_no | smallint | 否 | 1 | — | 版本号 |
| status | varchar(20) | 否 | 'DRAFT' | — | 评审状态（DRAFT 草稿 / FINAL 最终） |
| reviewer_id | bigint | 是 | — | FK → sys_user.id | 教师；AI 为空 |
| total_score | numeric(5,2) | 是 | — | — | 本次评审总分（0~100） |
| conclusion | varchar(10) | 是 | — | — | PASS/FAIL |
| comment | text | 是 | — | — | 评语/批注 |
| dimension_json | jsonb | 否 | '[]' | — | 各维度得分与理由（JSON 数组，不再单独建维度字典表） |
| ai_model | varchar(100) | 是 | — | — | AI 模型名称 |
| raw_json | jsonb | 否 | '{}' | — | AI 原始返回快照（JSON） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| finished_at | timestamptz | 是 | — | — | 结束时间 |

**表级约束**：
- `CONSTRAINT uk_review_record UNIQUE (submission_id, review_kind, version_no)`

**索引**：
- `idx_review_record_submission (submission_id, review_kind, version_no DESC)`


### 5.8 review_ai_job —— AI 评审任务

> AI 评审的异步任务记录，用于重试与对账。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| submission_id | bigint | 否 | — | FK → project_submission.id | 整单提交 ID（project_submission.id） |
| job_status | varchar(20) | 否 | 'QUEUED' | — | 任务状态（QUEUED 排队 / PROCESSING 处理中 / SUCCEED 成功 / FAILED 失败） |
| model_name | varchar(100) | 是 | — | — | 执行批阅的 AI 模型 |
| request_id | varchar(100) | 是 | — | — | AI 服务请求 ID（对账用） |
| error_msg | text | 是 | — | — | 错误信息 |
| attempt_count | smallint | 否 | 0 | — | 已尝试次数（用于重试与对账） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| finished_at | timestamptz | 是 | — | — | 结束时间 |

**索引**：
- `idx_review_ai_job_status (job_status, created_at)`


## 6. F 证书台账

### 6.1 student_certificate —— 学生证书/台账

> 已发放的电子证书与台账（作废、补发、有效期）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| student_id | bigint | 否 | — | FK → sys_user.id | 学生 ID（sys_user.id） |
| job_id | bigint | 否 | — | FK → job.id | 认证岗位 |
| certificate_no | varchar(64) | 否 | — | — | 证书编号（唯一） |
| cert_name | varchar(100) | 否 | — | — | 证书名称快照 |
| issue_org_name | varchar(150) | 是 | — | — | 认证机构快照 |
| issue_date | date | 否 | CURRENT_DATE | — | 发放日期 |
| expire_date | date | 是 | — | — | 有效期至 |
| status | varchar(20) | 否 | 'VALID' | — | 证书状态（VALID 有效 / EXPIRED 过期 / REVOKED 作废 / RESSUED 已补发） |
| issued_by | bigint | 是 | — | FK → sys_user.id | 发放人 ID（sys_user.id） |
| file_asset_id | bigint | 是 | — | FK → file_asset.id | 电子证书文件 |
| revoke_reason | varchar(255) | 是 | — | — | 作废原因 |
| replaced_by_id | bigint | 是 | — | FK → student_certificate.id | 补发后新证书 ID（旧证指向新证） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_certificate_no UNIQUE (certificate_no)`

**索引**：
- `idx_student_cert_student (student_id, status)`
- `idx_student_cert_job (job_id, status)`


## 7. H AI 问答知识库

### 7.1 knowledge_doc —— 知识文档

> RAG 知识库文档（岗位资料、项目评分标准等）。归属不在本表重复存：评分标准通过
> `file_asset → project_file` 找到所属项目，岗位资料将来加 `job_file`（纯增量）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| title | varchar(255) | 否 | — | — | 知识文档标题 |
| doc_type | varchar(20) | 否 | 'KNOWLEDGE' | — | 用途（KNOWLEDGE 知识问答 / EVAL_CRITERIA 评分标准，只供批改链路内部调用） |
| file_asset_id | bigint | 是 | — | FK → file_asset.id | 文件 ID（file_asset.id） |
| source | varchar(30) | 否 | 'UPLOAD' | — | 来源（UPLOAD 文件上传 / TEXT 手工录入） |
| description | text | 是 | — | — | 说明 / 描述 |
| status | varchar(20) | 否 | 'PARSING' | — | 文档状态（PARSING 解析中 / READY 可用 / FAILED 失败 / DISABLED 停用） |
| parse_error | text | 是 | — | — | 解析或切片失败原因（失败时给用户看的原因） |
| chunk_strategy | varchar(50) | 是 | — | — | 切分策略版本（如 v1_structural_800，换策略时用来识别旧数据） |
| total_chunks | int | 否 | 0 | — | 切片总数 |
| uploaded_by | bigint | 是 | — | FK → sys_user.id | 上传人 ID（sys_user.id） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |
| deleted_at | timestamptz | 是 | — | — | 软删除时间（为空表示未删除） |

**索引**：
- `idx_knowledge_doc_file_asset (file_asset_id)` —— 支撑"项目 → 附件 → 文件 → 知识文档"这条链


### 7.2 knowledge_chunk —— 知识切片

> 知识文档切片，向量库 Milvus 中的向量与此表 vector_id 对应。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| doc_id | bigint | 否 | — | FK → knowledge_doc.id | 知识文档 ID（knowledge_doc.id） |
| chunk_index | int | 否 | — | — | 切片序号 |
| content | text | 否 | — | — | 切片正文 |
| content_hash | varchar(64) | 是 | — | — | 内容哈希（去重 / 增量更新用） |
| char_count | int | 否 | 0 | — | 字符数 |
| heading_path | varchar(512) | 是 | — | — | 块所属标题路径（结构化切分产物，引用展示用） |
| page_no | int | 是 | — | — | 来源页码（PDF 等，用于引用定位） |
| token_count | int | 是 | — | — | 实际 token 数（接模型后回填） |
| vector_id | varchar(64) | 是 | — | — | Milvus 主键 |
| model_name | varchar(100) | 是 | — | — | 模型名称 |
| status | varchar(20) | 否 | 'PENDING' | — | 切片状态（PENDING 已切片待入库 / READY 已写入 Milvus / FAILED / DISABLED） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**表级约束**：
- `CONSTRAINT uk_knowledge_chunk UNIQUE (doc_id, chunk_index)`

**索引**：
- `idx_knowledge_chunk_doc (doc_id, chunk_index)`


### 7.3 ai_qa_session —— AI 问答会话

> 学生的 AI 问答会话。

> **保留期**：`updated_at` 超过 `ai.qa.history_retention_days`（默认 7 天）没活动的会话，
> 查询层不再返回（对外 404），并由 `scripts/prune_qa_history.py` 按会话粒度物理清理
> ——一个会话要么整体保留、要么整体删除，不做单条消息截断。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| student_id | bigint | 否 | — | FK → sys_user.id | 学生 ID（sys_user.id） |
| title | varchar(255) | 是 | — | — | 会话标题 |
| subject | varchar(100) | 是 | — | — | 课程/知识范围 |
| status | varchar(20) | 否 | 'ACTIVE' | — | 会话状态（ACTIVE 进行中 / CLOSED 已结束） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |
| updated_at | timestamptz | 否 | now() | — | 更新时间 |

**索引**：
- `idx_qa_session_student (student_id, updated_at DESC)`


### 7.4 ai_qa_message —— 问答消息

> **写入规则**：一次提问 = 用户一行（`status=COMPLETED`）+ 回答一行；回答行在流结束、
> 报错或客户端断开时统一落库（断开也保留已生成的部分，`status=FAILED`）。
> `prompt_tokens` / `completion_tokens` 只写在回答行上，供 `GET /api/qa/usage` 汇总；
> 兼容端点不返回 usage 时按字符估算回填，**不留空**（留空会让统计永远为 0）。

> 会话中的问答消息（用户 / 助手）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| session_id | bigint | 否 | — | FK → ai_qa_session.id | 会话 ID（ai_qa_session.id） |
| role | varchar(20) | 否 | — | — | 消息角色（USER 用户提问 / ASSISTANT AI 回答） |
| content | text | 否 | — | — | 消息内容（用户提问或 AI 回答） |
| model_name | varchar(100) | 是 | — | — | 模型名称 |
| prompt_tokens | int | 是 | — | — | 输入 Token 数 |
| completion_tokens | int | 是 | — | — | 输出 Token 数 |
| status | varchar(20) | 否 | 'COMPLETED' | — | 消息状态（COMPLETED / FAILED） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**索引**：
- `idx_qa_message_session (session_id, created_at)`


### 7.5 ai_qa_citation —— 回答引用来源

> 回答引用的知识来源，保证回答可信度与可追溯。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| message_id | bigint | 否 | — | FK → ai_qa_message.id | 消息 ID（ai_qa_message.id） |
| doc_id | bigint | 是 | — | FK → knowledge_doc.id | 知识文档 ID（knowledge_doc.id） |
| chunk_id | bigint | 是 | — | FK → knowledge_chunk.id | 知识切片 ID（knowledge_chunk.id） |
| source_title | varchar(255) | 是 | — | — | 引用来源标题快照 |
| snippet | text | 是 | — | — | 引用片段快照 |
| relevance | numeric(6,4) | 是 | — | — | 相关度（0~1） |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**索引**：
- `idx_qa_citation_message (message_id)`


## 8. I 通知与审计

### 8.1 notification —— 站内通知

> 站内通知（复审结果、技能点亮、任务发布、证书等）。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| recipient_user_id | bigint | 否 | — | FK → sys_user.id | 接收人 ID（sys_user.id） |
| title | varchar(255) | 否 | — | — | 通知标题 |
| content | text | 是 | — | — | 通知内容 |
| biz_type | varchar(50) | 是 | — | — | 业务类型（REVIEW_RESULT / STAGE_RESULT / TASK_PUBLISH / CERT / POINT） |
| biz_id | bigint | 是 | — | — | 关联业务 ID |
| read_at | timestamptz | 是 | — | — | 已读时间 |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**索引**：
- `idx_notification_recipient (recipient_user_id, read_at)`


### 8.2 operation_log —— 操作审计日志

> 关键操作审计日志（发布、撤回、审核、发证等），append-only。

| 字段 | 类型 | 可空 | 默认值 | 键 / 约束 | 说明（代表什么） |
| --- | --- | --- | --- | --- | --- |
| id | bigint | 否 | 自增 | PK | 主键 ID |
| operator_id | bigint | 是 | — | FK → sys_user.id | 操作人 ID（sys_user.id） |
| module | varchar(50) | 否 | — | — | 业务模块（如 PROJECT / PUBLISH / REVIEW / CERT） |
| action | varchar(50) | 否 | — | — | 操作动作（如 CREATE / PUBLISH / WITHDRAW / REVIEW / ISSUE） |
| target_type | varchar(50) | 是 | — | — | 操作对象类型 |
| target_id | bigint | 是 | — | — | 操作对象 ID |
| detail_json | jsonb | 否 | '{}' | — | 变更前后快照（JSON） |
| ip | varchar(45) | 是 | — | — | 客户端 IP |
| user_agent | varchar(255) | 是 | — | — | 客户端 User-Agent |
| created_at | timestamptz | 否 | now() | — | 创建时间 |

**索引**：
- `idx_operation_log_module (module, created_at)`
- `idx_operation_log_operator (operator_id, created_at)`


---

## 附录 A：枚举字典

| 枚举 | 取值 |
| --- | --- |
| user_type | STUDENT / TEACHER / ADMIN |
| 通用状态 | ACTIVE / DISABLED |
| class_student.status | ENROLLED / LEFT |
| project_level | BASIC（基础）/ ADVANCED（进阶）/ EXPANDED（拓展） |
| training_project.difficulty | 1~5 的数字（1 最简单，5 最难） |
| training_project.status | DRAFT / PUBLISHED / OFF_SHELF |
| student_project.status | NOT_STARTED / IN_PROGRESS / SUBMITTED / COMPLETED |
| training_attempt.status | IN_PROGRESS / SUBMITTED / COMPLETED / ABANDONED |
| project_submission.status | PENDING_AI / AI_PASSED / AI_FAILED / PENDING_REVIEW / REVIEWING / REVIEWED / WITHDRAWN |
| final_conclusion | PASS / FAIL |
| review_kind | AI / TEACHER |
| review_record.status | DRAFT / FINAL |
| review_ai_job.job_status | QUEUED / PROCESSING / SUCCEED / FAILED |
| student_skill.state | LOCKED（未解锁）/ ACTIVATED（已激活）/ MASTERED（已精通） |
| student_certificate.status | VALID / EXPIRED / REVOKED / RESSUED |
| knowledge_doc.status | PARSING / READY / FAILED / DISABLED |
| knowledge_doc.doc_type | KNOWLEDGE（知识问答）/ EVAL_CRITERIA（评分标准，只供批改链路） |
| knowledge_chunk.status | PENDING（已切片待入库）/ READY（已写入 Milvus）/ FAILED / DISABLED |
| project_file.file_kind | REPORT_TEMPLATE / DATASET / GUIDE / SCORING_CRITERIA（评分标准）/ OTHER |
| ai_qa_session.status | ACTIVE / CLOSED |
| ai_qa_message.role | USER / ASSISTANT |
| file_asset.biz_type | SUBMISSION / AVATAR / CERT_PDF / IMPORT / REPORT / REPORT_TEMPLATE / DATASET / GUIDE / SCORING_CRITERIA |

## 附录 B：外键关系一览

| 表.字段 | 引用 | 说明 |
| --- | --- | --- |
| sys_user_role.user_id | sys_user.id | 引用 sys_user 表 |
| sys_user_role.role_id | sys_role.id | 引用 sys_role 表 |
| sys_user_role.assigned_by | sys_user.id | 引用 sys_user 表 |
| sys_role_permission.role_id | sys_role.id | 引用 sys_role 表 |
| sys_role_permission.permission_id | sys_permission.id | 引用 sys_permission 表 |
| system_config.updated_by | sys_user.id | 引用 sys_user 表 |
| class_info.head_teacher_id | sys_user.id | 引用 sys_user 表 |
| class_group.class_id | class_info.id | 引用 class_info 表 |
| class_student.class_id | class_info.id | 引用 class_info 表 |
| class_student.student_id | sys_user.id | 引用 sys_user 表 |
| class_student_group.class_student_id | class_student.id | 引用 class_student 表 |
| class_student_group.group_id | class_group.id | 引用 class_group 表 |
| class_student_group.assigned_by | sys_user.id | 引用 sys_user 表 |
| job.created_by | sys_user.id | 引用 sys_user 表 |
| student_job.student_id | sys_user.id | 引用 sys_user 表 |
| student_job.job_id | job.id | 引用 job 表 |
| skill_node.tree_id | skill_tree.id | 引用 skill_tree 表 |
| skill_node_dependency.node_id | skill_node.id | 引用 skill_node 表 |
| skill_node_dependency.prerequisite_node_id | skill_node.id | 引用 skill_node 表 |
| student_skill.student_id | sys_user.id | 引用 sys_user 表 |
| student_skill.skill_node_id | skill_node.id | 引用 skill_node 表 |
| job_skill.job_id | job.id | 引用 job 表 |
| job_skill.skill_node_id | skill_node.id | 引用 skill_node 表 |
| project_skill.project_id | training_project.id | 引用 training_project 表 |
| project_skill.skill_node_id | skill_node.id | 引用 skill_node 表 |
| growth_rule.updated_by | sys_user.id | 引用 sys_user 表 |
| training_project.job_id | job.id | 引用 job 表 |
| training_project.creator_id | sys_user.id | 引用 sys_user 表 |
| project_module.project_id | training_project.id | 引用 training_project 表 |
| project_module.template_id | project_stage_template.id | 引用 project_stage_template 表 |
| file_asset.uploader_id | sys_user.id | 引用 sys_user 表 |
| student_project.student_id | sys_user.id | 引用 sys_user 表 |
| student_project.project_id | training_project.id | 引用 training_project 表 |
| training_attempt.student_project_id | student_project.id | 引用 student_project 表 |
| attempt_stage.attempt_id | training_attempt.id | 引用 training_attempt 表 |
| attempt_stage.project_module_id | project_module.id | 引用 project_module 表 |
| attempt_stage_file.attempt_stage_id | attempt_stage.id | 引用 attempt_stage 表 |
| attempt_stage_file.file_asset_id | file_asset.id | 引用 file_asset 表 |
| project_submission.attempt_id | training_attempt.id | 引用 training_attempt 表 |
| review_record.submission_id | project_submission.id | 引用 project_submission 表 |
| review_record.reviewer_id | sys_user.id | 引用 sys_user 表 |
| review_ai_job.submission_id | project_submission.id | 引用 project_submission 表 |
| student_certificate.student_id | sys_user.id | 引用 sys_user 表 |
| student_certificate.job_id | job.id | 引用 job 表 |
| student_certificate.issued_by | sys_user.id | 引用 sys_user 表 |
| student_certificate.file_asset_id | file_asset.id | 引用 file_asset 表 |
| student_certificate.replaced_by_id | student_certificate.id | 引用 student_certificate 表 |
| knowledge_doc.file_asset_id | file_asset.id | 引用 file_asset 表 |
| knowledge_doc.uploaded_by | sys_user.id | 引用 sys_user 表 |
| knowledge_chunk.doc_id | knowledge_doc.id | 引用 knowledge_doc 表 |
| ai_qa_session.student_id | sys_user.id | 引用 sys_user 表 |
| ai_qa_message.session_id | ai_qa_session.id | 引用 ai_qa_session 表 |
| ai_qa_citation.message_id | ai_qa_message.id | 引用 ai_qa_message 表 |
| ai_qa_citation.doc_id | knowledge_doc.id | 引用 knowledge_doc 表 |
| ai_qa_citation.chunk_id | knowledge_chunk.id | 引用 knowledge_chunk 表 |
| notification.recipient_user_id | sys_user.id | 引用 sys_user 表 |
| operation_log.operator_id | sys_user.id | 引用 sys_user 表 |

## 附录 C：通用字段约定

| 字段 | 约定 |
| --- | --- |
| id | 主键，`bigint` 自增；未来可平滑切换雪花 ID |
| created_at / updated_at | 创建、更新时间，`timestamptz`，默认 `now()` |
| deleted_at | 软删除时间；仅主数据表具备，流水 / 快照表不删除 |
| status / xxx_status | 状态 code，取值范围见附录 A，由后端常量管理 |
| sort_no | 展示排序，值越小越靠前 |
| remark / description | 备注与说明文本 |
