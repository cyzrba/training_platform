# 复制为 .env 后按需修改；留空使用默认值
APP_NAME=岗位闯关式实训平台
DEBUG=true

# 留空则使用 <项目根>/data/app.db
DATABASE_URL=

# 是否打印 SQL（排查问题时临时开）
SQL_ECHO=false

# 允许的前端来源，JSON 数组
CORS_ORIGINS=["*"]

API_PREFIX=/api/v1
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=200
