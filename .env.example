# 复制为 .env 后按需修改；留空使用默认值
APP_NAME=岗位闯关式实训平台
DEBUG=true

# 留空则使用 <项目根>/data/app.db
DATABASE_URL=

# 是否打印 SQL（排查问题时临时开）
SQL_ECHO=false

# 允许的前端来源，JSON 数组
CORS_ORIGINS=["*"]

# 新建账号的默认密码（只存哈希，不落明文）
DEFAULT_PASSWORD=123456

API_PREFIX=/api
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=200
